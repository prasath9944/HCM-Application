export interface ClaimSumbit{
  claimID:string,
  claimType:string,
  claim_Amount:number,
  remarks:string,
  claim_Date:string
}

export interface GetClaims{
  claimID:string,
  claimType:string,
  claim_Amount:number,
  remarks:string,
  claim_Date:string
}
